# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/

# Virtual envs
venv/
env/
.venv/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data — never commit raw or processed data
data/raw/
data/processed/
*.parquet
*.csv
*.pkl
*.joblib

# Outputs — charts regenerable, slide deck committed for portfolio
outputs/charts/*.png
outputs/charts/*.pdf
outputs/charts/*.svg
outputs/tables/*.csv
# Note: outputs/slide_deck.html IS committed — it's the main deliverable artifact

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Secrets
.env
.env.local
*.pem

# NOTE: INTERVIEW_PREP.md is now encrypted via git-crypt (see .gitattributes).
# The encrypted version lives in the repo; only your GPG key decrypts it locally.