# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/

# Virtual envs
venv/
env/
.venv/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data — never commit raw or processed data
data/raw/
data/processed/
*.parquet
*.csv
*.pkl
*.joblib

# Outputs — charts regenerable, slide deck committed for portfolio
outputs/charts/*.png
outputs/charts/*.pdf
outputs/charts/*.svg
outputs/tables/*.csv
# Note: outputs/slide_deck.html IS committed — it's the main deliverable artifact

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Secrets
.env
.env.local
.env.bak
.env.save
*.pem

# Personal notes — kept locally, never committed
INTERVIEW_PREP.md
SUBMISSION.md
LOCAL_SETUP.md

# git-crypt state (no longer in use)
.git-crypt/